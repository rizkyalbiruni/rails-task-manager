
<h2><%= @task.title %></h2>
<p>----------</p>
<p><%= @task.details %> - <%= @task.completed ? "completed" : "not completed" %></p>

<h3><%= link_to "edit", edit_task_path(@task) %></h3>

<h5><%= link_to "Delete", task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></h5>

<%= link_to "Go Back", tasks_path %>
